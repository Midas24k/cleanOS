{
  "name": "cleanos",
  "version": "0.1.0",
  "description": "Cross-platform system cleaner — macOS first",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron . --dev",
    "test": "jest",
    "test:unit": "jest",
    "test:e2e": "playwright test",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "jest": "^29.0.0",
    "@playwright/test": "^1.40.0"
  }
}
